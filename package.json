{
  "name": "ion-router-issue",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type":"module",
  "scripts": {
    "build": "esbuild src/index.ts --outfile=dist/bundle.esm.js --sourcemap --bundle --target=esnext --format=esm",
    "check": "tsc -noEmit",
    "serve": "wds --watch",
    "test": "wtr \"tests/**/*.Test.js\" --node-resolve",
    "test:watch": "wtr \"tests/**/*.Test.js\" --node-resolve --watch"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@web/dev-server": "^0.1.34",
    "esbuild": "^0.15.10",
    "typescript": "^4.8.4"
  },
  "dependencies": {
    "@ionic/core": "^6.3.1",
    "lit": "^2.4.0"
  }
}
